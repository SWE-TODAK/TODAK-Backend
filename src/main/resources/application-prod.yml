spring:
  datasource:
    url: ${TODAK_DB_URL}
    username: ${TODAK_DB_USER}
    password: ${TODAK_DB_PASS}
  jpa:
    hibernate:
      ddl-auto: validate # 운영은 validate 권장
  flyway:
    enabled: true #